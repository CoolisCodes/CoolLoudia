# CoolLoudia Core

This package contains the core scripts and assets for the CoolLoudia Unity project.

## Installation

Copy the `com.coolloudia.core` folder into your project's `Packages` directory or add it as a local package via the Unity Package Manager.
